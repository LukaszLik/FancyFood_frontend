import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#ffc107",
            light: "#fff350",
            dark: "#c79100",
        },
        secondary: {
            main: "#26c6da",
            light: "#6ef9ff",
            dark: "#0095a7",
        },
    },
});

export default theme;
